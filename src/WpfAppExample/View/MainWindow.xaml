<Window x:Class="WpfAppExample.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppExample"
        xmlns:pdf="clr-namespace:MoonPdfLib;assembly=MoonPdfLib"
        xmlns:vm="clr-namespace:WpfAppExample.ViewModel"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <StackPanel>
            <Button Content="Open Source" Command="{Binding OpenCommand}"/>

            <Button Content="Open File" Command="{Binding OpenCommand}"/>

            <pdf:MoonPdfPanel Source="{Binding Data, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>
    </Grid>
</Window>
